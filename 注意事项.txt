260303
1.实装后的深度图像校准部分，未实际安装conda运行环境，也未对环境进行备份
2.程序执行后，json文件被修改，但是未重新读取（暂时问题不大）
3.新实装的HoloLens2数据下载和深度图像校准部分代码未执行验证过

260309
server环境需要安装
pip install numpy opencv-python av requests
pip install pyzdepth

260315
1.模型生成已写道sam3，未完成环境相关config的内容填写，可能未搭建完环境
2.需要把除了最初的json写入外的所有流程放到task_worker.py里
3.生成taskid或许不用这么多东西
4.taskid的列表和状态或许可以写到本地文件里，最少需要taskid，对应json，状态

260315-2
1.未完成sam相关环境配置，blender不知晓
2.缺少重定位和大小缩放相关代码

260317
在该文件中，使用probreg，写一个获得可以把模型缩放到正确大小和旋转姿势，并且有正确位置的代码，该代码接受task_worker管理，在objectalignment步骤中被使用。该代码接受json地址，使用"PVCamera""k"获取将要使用的，用于深度图投影的相机内参矩阵，"PVCamera""pose"是设备位姿

260328
1.生成的模型，现在在计算结束后是显示在眼前的，就像是在拍照时的视角的镜像（以模型中点为中心）。似乎是由于拍摄时得到的，以当时的相机为原点的点云坐标数据，被直接应用到了新的相机位置和姿势上。这就导致了显示在正前方的模型，看着很像是在模型的对面的位置拍摄的感觉。
2.最后的那个土拨鼠玩偶，似乎因为距离过远导致depth相机数据出现问题（怀疑距离1m左右）
3.在尝试进行第二次拍照时，出现不明崩溃，这个崩溃也见于同步启动相机时，或者上传等待时间过长时。
4.怀疑可能导致崩溃原因
[worker] completed task: 33383d81-80f8-4d65-8120-d7feb836bc4b
10.40.1.132 - - [28/Mar/2026 08:22:50] "GET /check/?task_id=33383d81-80f8-4d65-8120-d7feb836bc4b HTTP/1.1" 200 -
10.40.1.132 - - [28/Mar/2026 08:22:50] "GET /files/fbx/20260328_082037_577011Z_sam3_color.fbx HTTP/1.1" 200 -
[ERROR] /generate exception: 413 Request Entity Too Large: The data value transmitted exceeds the capacity limit.
10.40.1.132 - - [28/Mar/2026 08:27:06] "POST /generate HTTP/1.1" 400 -

260409
1.在hololens启动时加上时间戳获取，每次上传时同步传下，作为这次启动的标志，为之后使用mark进行世界坐标保存做准备
2.在server上加上识别对应的模块，先判断框选范围内有无完整的这个标，如果有，在当前一直在跑的taskworker旁路进行坐标重计算
3.坐标重计算需要修改数据库结构，加上以启动时间戳为基准的部分，向内写入mark的坐标（作为多次启动时模型加载的基准位置）
4.修改unity和server相关代码，允许使用上一次数据

1.0
1.增加shigurei的位置记录
2.unity增加滑块，物品表，可以放置物体并进行位置追踪
3.朝向是个很大的问题，估计无法理解朝向（待议）

260412
1.config里增加可选渲染结果选项（对于instantmesh的video生成也是一样）
2.按照目前的预览渲染的链路（包括模型导入，fbx生成，icp结果等相关坐标变换），重新检查最终的模型位姿变换结果

260414
我修改了一部分的pvcamera的旋转矩阵提取四元数后的z反转问题：“def compute_world_pose(task: dict) -> dict[str, list[float]]:
    ...
    world_quat = rotation_matrix_to_quat_xyzw(world_rotation)
    world_quat[2] *= -1.0
    world_quat = normalize_quat_xyzw(world_quat)”和“def serialize_pose(rotation: np.ndarray, translation: np.ndarray, coordinate_basis: str) -> dict:
    ...
    quat_xyzw = rotation_matrix_to_quat_xyzw(rotation)
    quat_xyzw[2] *= -1.0
    quat_xyzw = normalize_quat_xyzw(quat_xyzw)”
但是好像只修改了相机的部分，并没有修正物体的部分。按照这个，继续修改这个链路中的相关项（只反转这个z值，不要做任何其他事情）。

260423
1.修改文件目录以及git和dvc追踪文件夹：sotsuken-hololens2-system\data\aruco内改成一个git追踪的包含原始marker图片ar_marker_7x7_1.png和数据的aruco.json的文件夹，和一个dvc追踪的放计算数据文件夹，并在setup-dvc-gdrive.txt中的日常流程：上传：中进行修正
2.清理git追踪的sotsuken-hololens2-system\data\aruco内历史数据，直接从最新版开始追踪
3.检查database里的数据，指出并修正程序中错误的地方
4.在拍摄距离过远时，显示上传文件过大，深度怀疑是深度图片的问题，尝试在网上搜索相关问题并修正
5.database里有一次好像出现图片里有marker，却没识别出来的情况，这种应该怎么解决？新增一个marker按钮吗？

260423-2
说明：近距离AHAT的“距离过远/上传文件过大”拦截目前先保持不启用；相关代码只保留深度清洗与统计，不主动阻止上传。若之后要恢复拦截，需要同时重新启用Unity端和server端的AHAT上传拦截。
1.深度清洗似乎还是解决不了，会被统计拦截
2.急需添加相关按钮专门进行marker适配
3.check依旧无法停止，可能需要重写这块的停止条件等
4.加载最新一次含有有效marker数据的那块还是有问题，似乎写成加载上次有效数据了

260427
1.在拍摄mark后，unity还是一直在check，但是本地有坐标系了，看看check的流程和终止条件
2.setting没有正常显示，怀疑unity没有绑定啥的

260427-1
1.可改进：
1.1.marker的微小误差会导致模型位置出现巨大变动
1.2.本地只能存储一个模型，只能历史访问一个模型
1.3.缺少long深度相关整个的流程
2.之后需要：
2.1.接入项目数据库绑定，历史位置查询功能
2.2.最重要的是需要能够知道这是同一个东西
2.2.1.预计要使用shigurei的别人的项目，需要能够发送图片+文本（或者声音），服务器获得查询结果。
2.2.2.shigurei需要深度图进行icp，以确定位置和旋转角度（根据深度图精度，怀疑还需要渲染比对），但是时间会很长
2.2.3.可以先简单的放在那里，icp扔后台另一个慢慢算。也可以稍微聪明点，预先算好，物体移动后就开始算
2.3.shigurei访问

260430
1.电脑和服务器实装shigurei
2.为了获取shigurei的记录数据，需要目前marker为基准的坐标系，和shigurei为基准的深度数据+图片位置的两个变换函数
3.服务器应该会记录以shigurei为基准的位置（图片位置和深度）以及时间，作为和shigurei的同步依据
4.（shigurei的rgb摄像头和depth摄像头坐标系是否统一需要调查）
5.hololens2实装手指的位置（指尖延伸到障碍物位置，之后以次为基准向上半球搜索）
以此应该可以最基础再现，手指位置附近曾经存在的物体
（但是需要考虑数据库范围，可能需要指定数据范围）
6.模型面数可缩少调查
7.icp点数可简化
8.再贴几个marker（待议）

260502
这部分功能目标是可以在现实中指一个位置，然后显示历史里曾在此位置的模型（历史拍摄并建模的模型）
1.新建database表，用于存放模型在世界中的包围盒信息（AABB）（在上传生成时）
该包围盒关联的database的表应包含上传时间，模型名称，包围盒8个角点位置（以armarker（统一坐标系）为基准），三个部分
2.在unity端，新建脚本，挂载需要你告诉我怎么做。
该脚本目的为，只允许右手（或者左手），在unity界面可选但是固定，发射射线到终点平面（现实世界障碍），程序将在unity打开时强制该射线显示。
程序将会在某个函数触发后，在等待3s（unity可设定时间）后获取当前时间下的，食指起点坐标，射线方向，射线终点坐标，上传服务器解析。
3.服务器解析食指起点坐标和射线方向，以终点坐标为截止位置（如无，设置最远距离10m；或者已经超出指尖到最远模型角点距离），路径上碰到的第一个模型的包围盒的六个面中的任何一个（内外都可）。
4.对包围盒设置两个检索标准，一个是默认的最新5个（unity写死），另一个是某段时间之内（两个函数，但是第二个写完留着就行）
5.该功能和之前的armarker相关请求，我希望可以另开一个worker解决（高优先级，并行），所以需要对于生成中的模型，准备好锁来防止生成时出现armarker同时更改数据库同一个位置（虽然感觉影响不大，或许单纯的标注为未完成的这种，armarker直接跳过，等生成完了自己调重写就行？这样marker更新也就问题不大（帮我检查这部分思路））
6.加一个函数，该函数为开关（点一下开，再点一下关），包围盒计算虽然在云端完成，但是我还是希望在调试时，本地可以看到。
在该开关打开时，我可以看到检索范围内的包围盒位置（服务器下载最新，并且设置为显示）。关闭时记得清除。


260505-1
debug
1.hololens自带的射线正常显示，但是另一枚射线（直线）追踪的太好了（太容易动了，并且没有在平面位置正确停止），我不需要这条射线，以hololens的右手默认的那条为准（现在hololens2只手都默认的有射线，只有右手有一个白色光柱，非常灵敏的，指尖发出的）（两条hololens默认的射线保留）
2.resest selectionbox，点击仍旧没反应，我希望是在弹出这个窗口后，能改变窗口位置（毕竟窗口可能在不好点的位置）
3.点击左手设置菜单后，非常卡，甚至程序感觉直接无响应了，之后过一段时间又好了；拍照倒计时结束后，也突然感觉像程序无响应，我的手部默认显示的东西都没了。
4.在上传两个模型待处理（处理中）后，直接上传armarker，显示错误，数据库里也没有第二次的模型有关的任务数据以及后面的armarker数据（armarker可能是程序设计如此，但模型生成不应该）（我的本意是可以依次在未完成状态上传多个图片到服务器队列处理，并且可能在某个时候上传下armarker进行坐标标定，处理完成后自动返回数据（armarker优先度高））
5.射线没有倒计时，结束后一直显示nohit（明明hololens的那条射线已经打到模型上了）

260505-2
1.reset selectionbox 指的是重置（Canvas Selection box）；设置窗口（CanvasSettings），还是点一下开，再点一下关，开的时候放在眼前0.8m处的设定
2.我希望taskworker不是一条跑到黑，而是在一个阶段结束，直接切到优先级最高的上面（注意保留armarker的最高优先级）
3.增加接口，清除本地保留模型（给出位置和名称，我随后加上）
4.现在hololens在上一个内容处理未结束，请求新的内容后，旧的内容无法自动加载（这个需要商定计划讨论），并且有一个box，没有正确显示（有没有可能当时没有拍armarker？），这个应该可以自动补上的。

260505-3
解决如下问题
1.reset selectionbox 指的是重置（Canvas Selection box），在拖动方块选择范围阶段，无法重置位置
2.扔回队列是扔到队列最上面，这样可以保证优先处理完成一个，并且可以在有armarker后，优先处理armarker
3.armarker生成结束，返回数据不显示armarker的原点的那个坐标轴了（准确来讲，无法正确接收到返回的数据）

260505-4
疑问：最终的那个box，是从那个结果里生成的，instantmesh还是后面抽象的那个
讨论如下修改
1.instantmesh生成的3d模型有的时候在某个模型外一点有杂音（突然就那一小块），帮我检测并且删去（在instantmesh生成模型后）
2.把icp部分，换成另一块（使用预留的那个跳过选项进行切换），这一块逻辑如下：
根据摄像机距离，和推测物体模型的缩放结果（会得到一个新的包围盒），去假设这个距离是包围盒前表面，进行模型放置（在摄像机到图片点云重心的延长线上，延长距离根据包围盒估算，试试厚度的一半）

260507
1.射线发射方向很有问题，似乎是视线前方而不是手指位置（hololens射线默认就这样吗）。
2.射线碰到空间上的障碍物，没有停留在表面上而是穿过去了（有的有反应，有的没反应）。
3.现在还是没有办法在armarker生成结束后自动下载，我通过 showlastmodel的逻辑成功活得到了marker坐标并显示，检查armarker问题。
4.我不希望在selection关闭时，点击resetselectionbox出现任何反应，只在打开时按照现有逻辑放置，也不需要操作开关；需要操作开关的是setting按钮。

260511
1.icp阶段（也可能是pose）还是没有明显加速
2.有一个模型生成结束后，hololens没有自动下载模型（在同时上传两个模型进行处理的时候）（多个模型生成结束也是没有自动下载）
--前俩未验证
3.在现有流水线上，加上切换sam3d的逻辑并且切过去（sam3d好像出的是ply点云，可能需要继续验证以及商议）。

260511-1
（已更新数据库）
1.拍摄armarker后，服务器已经显示已完成，但是本地还是显示start，也没有下载，也没有显示。
2.在该情况下，点击pointobject后，自动下载并显示了历史的5个模型，这不是我希望的（但是armarker显示了出来），之后清理所有模型缓存后，又变正常了
3.在有本地armarker坐标的情况下，连续拍摄两个模型，并没有正确下载，一直在model_polling_start
4.但是生成结束后，用手指显示那个能正常下载

260511-2
1.点击resect selection box 不应该关闭已经打开的selectionbox（去掉那个管拖动方块上传图片加box的那个的点击关闭的逻辑）
2.程序启动时没有在头上生成那个坐标轴（最新一次开始出现该问题），同时armarker依旧在卡。
3.在第一次尝试手指方向显示时，下载了armarker，然后设备启动位置以及armarker都出现坐标轴了。
4.随后可以正常下载并显示对应的模型
5.show last model还是希望显示历史最新的全部5个（其他好像没有问题）
6.模型生成完成后依旧没有自动下载。这块我怀疑和armarker的自动请求逻辑都有同一个问题。
7.补充：自动刷新跨 session，但是只能刷新本地已有的（正在显示的），没有的不要动。
8.补充：上次额度用完了，不知道改到了那里，综合git仓库记录和上次的记录，继续改

260518（待解决）
1.不知道为什么，新加的输出显示使用设备为cpu，日志如下
[STAGE] object_alignment : /workspace/data/upload/20260518_090325_198433Z_meta.json
[INFO] object_alignment : backend=cpu alignment_mode=foundationpose icp_enabled=False points=43474/3600 model_fit_points=0 discarded=3548 scale=0.225904 confidence=0.921 rmse=0.007180 preview=not-generated
[INFO] pose-local-rh   : pos=[0.003672159742563963, -0.02133191004395485, -0.5438885688781738] quat=[0.266676190029047, -0.035103126283652746, 0.010228804802634116, 0.9630923900379522]
[OK] object_alignment
2.需要后续引入shigurei

260521
1.现在对sam3dobjects的仓库内容进行了修改，我还在 SAM3D 子仓库里加了一个很小的兼容补丁：让 inference_pipeline.py 尊重外部 ATTN_BACKEND/SPARSE_ATTN_BACKEND，否则它在 A100 上会强制切到 flash_attn。flash_attn 在这个 CUDA 11.8 环境里源码编译太久，所以当前验证用的是 sdpa 后端。
我不希望对这个的源代码进行修改。
2.希望清理旧的sam3d-object conda，并且清除除了模型文件外不必要的缓存
3.我希望在config内增加模型生成切换到sam3d的开关，并按照现在的习惯写新的代码
3.1.sam3d的生成模型，似乎有透明度，这导致模型mesh不止一层（这个问题不用解决）。除此之外，生成的mesh面数过多，需要你参考国王的单层mesh的面数量，进行mesh面数量减少。同时要保证图片映射不出问题（如同现在instantmesh流程一样）。
4.把本地仓库推到远端

260523
1.现在的sam3d的降低面数走的是obj的，和instantmesh同样的逻辑。从这套逻辑改到走fbx（把这两个作为从主线上分离的两个分支，分别接到instantmesh和sam3d的后面）。无需兼容旧代码。这个面数，指的是和instanmesh一样的网格本身。降低比例先定为1/16，同样先清理碎片。现在的处理结果，处理前的obj能看得过去，但是有黑色的部分（似乎是缺失），我希望可以去掉。烘焙后的，直接变成一大块一大块的了，我不希望这样。
2.希望清理旧的sam3d-object conda，并且清除除了模型文件外不必要的缓存
3.把本地仓库推到远端

260523-2
1.sam3d的图像mesh压缩不理想，有部分色块颜色本来是黑色，但是通过某种方式变成了别的颜色（有违和感的部分地方），还有部分地方似乎由于当初降面的时候，似乎由于缺损补全，导致那块的颜色出现比较驳杂（比如这个炸弹的眼睛附近的斑驳色块，这个在原先是没有的）
2.需要后续引入shigurei

260525
1.关闭sam3d的面数降低部分，缩放留着。

260528
1.服务器内部严格使用同一种坐标系进行计算，各个部分的实际接口才进行坐标系换算，坐标系定义关系，换算关系等，全部写入同一个python文件。抛弃历史兼容，但要求结果和现在完全一致。
2.清除代码中无用的部分（比如有关sam3d的网络等待等，历史遗留兼容问题的部分）。
3.为现在的代码抽出设计，放置等习惯，并保存为文档。之后有新功能加入时，遵守该文档要求，至少做到文件放置位置统一就行。

260528-2之后计划
1.查询Foundationpose为什么第一步需要那么长时间
2.创建demo视频，关于物体不在后的操作
3.之后的预定，或许可以通过触摸那个模型进行一些操作

260528-3重要
1.修改坐标轴相关的东西后，hololens下载模型后，本地显示的红色的轴是反着的（这个轴的颜色和手性依照unity定义）（视觉上体现为，当初拍摄的摄像机视角下左右镜像翻转）
2.unity内放置的模型改为不可移动

260601
1.修改foundationpose的初值（可指定）
2.foundationpose调用时的输出给去了
3.把instantmesh搞到多个gpu里同时跑（config里设定最高数量）
4.sam3和foundationpose看看可不可以一次加载模型跑多个任务（多任务提速）

拆分worker，把instantmesh，sam3dmask，foundationpose放到不同的worker内。instantmesh可以多开，多个gpu里同时跑（config里设定最高数量）。sam3dmask：只要队列里还有任务没过 sam3mask，就保留 SAM3，如果没有任务5min左右，就会自动释放（config里设定最大时长）。foundationpose同样逻辑。sam3d_objects先不要管，能保证跑通就行。foundationpose调用时的输出给去了。

arucomarker的行为是什么，依旧会保持高优先级吗？

之后的测试，需要你复制出最新的一份数据，在不改变源数据的情况下自行测试（不跑完整主逻辑就行，内部的计算依照现行代码独立计算，不改原始数据（不动数据库，中间文件和json记录不输出覆盖应该就行））。
我需要知道以现在导出的instantmesh的默认导入轴后（注意需要加上初始缩放+位置移动），与foundationpose计算结果差多少。不用过于关注小误差，instantmesh的初始结果应该是大致可信的，需要关注的是缩放完成后的模型本身的坐标轴导入时的不同的地方。我的目标是，在保证最终坐标和旋转结果不变的情况下，使用一个初始值，加速foundationpose计算。由于我的instantmesh的导入导出坐标轴逻辑可能是乱的，所以或许可以修改instantmesh的部分（注意修改后需保证最终结果一致）。
在你计算并修改完坐标轴相关后，参考过去5次的结果，帮我看下具体的误差多大（注意不要修改原始数据）。随后把foundationpose改为依照现行初值（位置，instantmesh默认旋转），在一定范围内进行搜索。这个时候，稍微修改下缩放，分别对90%，95%，100%，105%，110%跑，找到最好的那个。

我需要你确认现在的整体链路是否可行
备份最近5次的数据库内容和数据到新的文件夹，然后调用主进程从头开始重跑。之后和备份数据对比，给出各阶段时间和参考意见。最后使用旧数据覆盖新数据。

260601-2
之后我想添加关于点击模型显示是谁动过的功能，但是现在的shigurei还没有校准坐标轴，所以只是评估方法。
shigurei保存一段时间内的rgbd，大概1min，每秒5张，在服务器启动后开始自动获取。
同步还要获取物体box，人体骨骼（应该是可以通过订阅获得数据）。
hololens上传计算后，应该可以通过坐标变换到同一个物体的box内（误差不会很大，限制数量设定为历史最大5个（以能匹配到的hololens上传的数据为准（也就是说，有拍摄到的3d模型的为主）），假设从拍照上传后，暂时没有被动过）。
根据shigurei获得到的深度数据，计算是否被遮挡（限制下面的移动判断）。
根据box是否还在原先的位置，判定是否被移动。
如果判定结果为确实被移动，如果有人体骨骼，根据骨骼的手碰到box那张图片和骨骼保存下来。
准备这个骨骼传到hololens以使得人体模型姿势改变（这个需要我之后考虑具体怎么导入）。
甚至可以综合foundationpose，生成整体运动轨迹+人体动作（之后考虑）。
如果没有骨骼，那就按照box消失后的那帧作为判断标准，仅记录图片。
hololens可以在点击模型后弹出一个按钮窗口，该窗口有3个按钮，功能是谁拿走的图片，人体模型，播放动画（后两个是如果可行的可选项）
由于shigurei的box不太理想，总容易掉线，或许需要考虑别的。或者先凑活着，把判断条件放宽。


260603
1.以/code/.test/overlay_model_on_ros_rgb.py为基础，写一个可以同时融合多个历史3d模型的脚本，这个模型需要使用blender，需要有贴图，之后以最新的5次数据跑这个。
需要以此校准shigurei相关导入的坐标轴约定。
2.test中关于新流程的20260601_035839_243537Z数据明显变差位置差约 0.444m，RMSE 0.0161 -> 0.1372，不可接受，需要重新评估。

2260603-2
1.overlay_fbx_models_on_ros_rgb.py已经可以输出可信结果，有遮挡和不遮挡模式，遮挡模式好像有点小问题，挡的地方缺一块，但是说得过去。
2.overlay_model_on_ros_rgb.py理论上和前一个的坐标轴变换语义是一致的，并且实际看下来也是问题不大的。但是不知道为什么中间出现了一个非常大的x状部分。
3.也就是说，现在的fbx+历史数据的坐标和旋转映射，根据第一个脚本来讲，应该是可信了（如果用obj那个也能用的话就更好了，感觉还是从obj的规则抽出来比较好）。
4.

260604
1.instantmesh，改为启动一个，但是允许批量处理的类型，和另外的哪个foundationpose一样，常驻5min。
2.
我想添加关于点击模型显示是谁动过的功能.
先评估方法。
在模型生成阶段的画box阶段，会有一个立体的box。通过这个box，可以依照文档中记录的坐标变换约定，投影到shigurei视角（中规中矩就差不多）。
启动一个sam3的视频追踪，使用这个投影后的box作为一个新的（多开）state，进行各个box独立追踪（不要往同一个里面新加，容易出事）
shigurei在本地姑且保留1min，每秒5张图片的所有节点的数据。
综合sam3的mask和shigurei的深度图，判断物体是否移动，还是只是被遮挡（这种情况下目标应该暂时丢失）
如果判断为移动（可以设定一个相对于初值的阈值，比如10cm？不知道精度够不过，抖动如何），那么输出变动开始的那一帧的mask（选开），输出时间。
使用时间查ros本地缓存的历史，长期化保存那一帧的数据（找个统一的地方复制出去），然后把3d模型和这一帧联系起来。
这样至少就有这一帧的变动时图片和当时人的骨骼（可能有）。
骨骼数据应该在shigurei的/workspace/code/.test/ros/20260603_071637_758712里面有参考。我需要的是，手部离box最近的，甚至是进入box范围内的哪个骨骼数据。我需要你在这个处理过程中对这个进行特殊标注。

之后，我在hololens端进行完拍照上传，完成模型后，就可以追踪到底是谁拿走的物体。
这个追踪，由于服务器不常驻，暂时只管当前启动次就可以。
点击模型触发，面前弹出窗口（窗口位置放到模型box前15cm就行，大小参考相机实时显示窗口大小），显示那张变动时的图片。同时显示那个骨骼（在hololens中以点线简单显示即可）
再次点击模型关闭。数据点击时下载，关闭时先不删除，只有那个删除所有模型的那个按钮才能删除。


对于手部遮挡这个问题，sam3应该可以连续追踪中去掉被遮挡的部分，这个时候深度可以暂时以新的mask判断是否还在原先位置（和前面的完整的同样部分记录对比）。
随后又变成完整的话，如果之前的部分仍旧在新的下变动不大，之后直接以新的覆盖。这个可能需要一个最小的计算范围，并且放一个合适的误差。
新建一个视频的tracker就行，并发的话，鉴于现在的并发比较多，我希望能根据网上资料，给每个模型（instantmesh，foundationpose，sam3的两个mask追踪）一个比较大的需要空间估计，然后根据现在那个显卡比较空，放得下，放进去。
shigurei的具体是谁暂时不需要关系，有图片和对应骨骼就行。
事件起点用“手进入 box 或最接近 box 的第一帧”

这几个相关的新的功能作为一个新的模块，放到stages里的一个新的文件夹里
event只需要记录这一个拿走就行，拿走判定后，结束对应的sam3的视频追踪。

+1
shigurei的缓存时间延长到10min，同时控制缓存的部分，只缓存必要的部分以降低存储空间压力。这样sam3就可以等待模型生成完毕后，从历史数据开始干道最新的，以弥补模型生成这段时间无法追踪的问题

260604-2
1.unity存在如下警告，我觉得问题不大：
Game scripts or other custom code contains OnMouse_ event handlers. Presence of such handlers might impact performance on handheld devices.
UnityEngine.GUIUtility:ProcessEvent (int,intptr,bool&)
2.shigurei的追踪（新增加的模块相关）
[STAGE] model_bounds : /workspace/data/upload/20260604_031202_386908Z_meta.json
[INFO] model_bounds : status=ready
[OK] model_bounds
{
  "status": "skipped",
  "reason": "not enough cached Shigurei frames",
  "frame_count": 0
}
[OK] model_event_tracking
shigurei没有正常记录，或许参考code/.test/save_latest_ros2_frame.py有帮助。


260605
1.sam3的video不要做那么多的缓存输出，只保留mask和人拿走的部分就好，空的可以不用存（为了限制所需的存储空间）。同时实时判定是否拿走，拿走判定后可以停掉sam3的视频追踪。
2.对于拍摄时遮挡的问题，我认为可以综合mask框选部分+深度画像解决。我认为在遮挡时，mask会过小，甚至消失，但是这个由于不知道具体的mask有多大，所以不太好适于判断。但是同时，遮挡时，方框内的范围，应当有很大一部分深度在模型的位置之前，我觉得可以从这里出发，继续在之后尝试mask操作。
3.这里就有引入问题，长期的尝试mask对于sam3当前的cpu处理是不是过重了，或许可以限制1s一帧的速度尝试绘制，或者在绘制前先检查box范围内的深度图像是不是有遮挡的很厉害（这个综合box和深度图，还有物体实际相对于shigurei的位置计算就行，可以放在sam3的videomask前，或许可以减少mask生成部分）

260605-2
1.问题1，遮挡，指的是那个投影的选框范围内有遮挡，不是mask的遮挡。
2.问题2，2450ff0d-0f6d-4c0e-8c8c-1aea07bc1d9a这种完成项目的debug的那个投影选框需要正常输出，但是问题在于这个mask完全在错误的物体上。
3.问题3，sam3的视频追踪时间问题，我希望能在有3d模型生成任务进来后，初始化im，sam3d的mask，sam3d的video，foundationpose，这四个模型，完成一个阶段自动推到下一个阶段，然后下一个从im继续跑
基于以上问题，帮我再不动shigurei的数据的情况下，修正代码，重跑最近4个。

260605-3
1./workspace/data/output/model_events这里面的文件夹希望使用时间戳那种名字（类似20260224_052438_050181Z）
2./workspace/data/output/model_events/5da084d5-29b3-4a92-b2e1-107b514118a2/sam3_video_runs/809b53bdcfba/frames/000088.jpg为什么是noevent，这一帧明显开始移动了
3.我说的那个开头的mask不全的遮挡判定，应该以投影到shigurei的那个选框为准，如果选框内的深度图，在某个时间内都在物体应该在的位置前面（有一部分深度就可以），并且在一定时间内，这部分选框内挡在前面的出现变动，那么就继续往后找，直到挡在前面的不太变动，或者没有挡在前面的，再把图喂给sam3video。
4./workspace/data/output/model_events/340bafa4-65df-4898-b100-8abe6a60db59/sam3_video_runs/48153e445d77和/workspace/data/output/model_events/340bafa4-65df-4898-b100-8abe6a60db59/sam3_video_runs/d9e934954150为什么会出现两个mask？data/output/model_events/340bafa4-65df-4898-b100-8abe6a60db59/sam3_video_runs/d9e934954150这个是对的（虽然有缺损）

260605-4
1.以/workspace/data/output/model_events/20260605_092623_759164Z/sam3_video_runs为例，似乎sam3video在物体丢失后会尝试找个新的替代？
这导致后续出现各种严重错误。所以我认为，需要对每一帧应用起那面的box范围内是否有过多遮挡（这个可以把遮挡部分阈值搞得更高些）遮挡是否稳定无需考虑。
2./workspace/data/output/model_events/20260605_092716_228601Z/sam3_video_runs/current/masks这个目录怎么这么复杂？去掉current这一层。并且这个后续的mask怎么突然开始错乱了，还被错误判断为被移动
3./workspace/data/output/model_events/20260605_092639_399392Z为什么mask了外部而不是正中央的部分。对于这个问题，引入正点。在depth稳定后，计算模型在shigurei视角的遮挡关系（只需要计算选框内范围，精度低点也可以接受，速度优先，但不能出现离谱的错误）之后从这个遮挡关系出发，从内部均匀随机不太靠边的部分（别贴边就行），选取（3-5个）正点，辅助sam3video初始化的追踪
4.由于专注于是否被拿走，所以从根上来讲，就不会出现mask突然瞬移这个问题，所以mask不能简单以最高的那个为主。并且前面说加入高遮挡的部分不给sam3，有一定可能在高遮挡时拿走，这种也好解决，拿走后物体就没了，mask也得变（至少原位置没了），图片就按前一个输入（进入高遮挡前）就行？。

260605-5
（仅评估可行性）
1.由于sam3的video非常不稳定，并且实际对于该是否移动，用处不大。所以决定完全废弃sam3video追踪路径。保留shigurei自动启停，清除sam3video相关代码。
2.新的想法如下，尝试参考/code/.test内的，有个fbx投影的那个脚本（这个脚本是对的，并且坐标轴变换逻辑由这个放到了docs里面），评估可行性。由于我只需要知道“拿走”，这一个状态。
所以在已经有可信模型的情况下，完全可以以这个模型投影到shigurei视角，作为mask，覆盖到深度图上。
而我既然有可信的3d模型，那么我完全可以使用这个已经具有世界坐标系下的模型+shigurei的深度数据，作为参考，即shigurei视角下的，模型表面的深度部分，应该能在shigurei的深度图中基本重合（或者误差不大）。
现在遇到的问题，多是前面被遮挡。
于是可以大胆假设，shigurei的深度无法看到模型背后的部分，即mask覆盖范围内的，深度图，深度应该不会深于3d模型前表面太多。
前面遮挡的话，shigurei的深度会在更前面。
所以只需要保证mask范围内前表面的平均深度过深（深于3d模型后表面平均深度），就可以认为已经被拿走。

被拿走判定：正确深度mask范围内深度平均值深于同样mask范围内的，3d模型后表面深度平均值
正确深度mask范围（随时更新）：3d模型投影mask范围内，和3d模型的前表面的shigurei投影距离接近，或者明显远的地方（大概可以认为小于投影到shigurei视角模型前表面的mask范围内，深度-10cm？（这个有待商榷，可以写到config）起始的范围）
画面被遮挡无需额外处理，正确深度会自动更新，去除被遮挡部分。

其他的有关抓取判定相关，如果被判定为抓取，那么就查手部骨骼最接近模型的，然后标记或者记录那个人体骨骼数据，按照当前流程准备好shigurei图片和骨骼，以备点击上传

260605-6
1.把正式代码中改为前表面后移10cm，
2.调整输出图片，输出可信区域距离上次变动超过整体的20%的融合图片，黄色可信，绿色完整，红色被移动。最终图片分为原图和融合判定的骨骼两种。输出除此之外还要有那个骨骼。原图和骨骼用于hololens的显示。剩下的扔debug里。其他的不输出
3.跑四次结果，时间不足的话就停止，给我报告

260608
1.在/workspace/code/reconstruction里面放sam3d-body子仓库，由你主导安装一个专门的conda环境，把模型放到huggingface的默认缓存目录下，建立软链接，并测试是否可运行
2./workspace/code/.test内写一个测试用代码，调用/workspace/data/shigure_event_cache/frames内的原始图片，实际任务来源在/workspace/data/output/model_events内的四个。相机参数不用使用估计，使用实际有的。目标是检测所有人物，推测人体mesh模型，放回3d空间内，算手腕距离物体最近的（一个可信范围内），然后输出mesh的覆盖rgb图（使用blender）。距离的话，由于是rgb-d图，所以使用第一步分割结果内的深度简单计算就可以，可以假设sam3dbody的旋转是大致可信的
3.已完成sam3dbody的导入生成验证，暂未实装到主逻辑链路

260608-2
1.对于拿取判断来讲，目前还没有定义背身完全覆盖物体时拿走的逻辑。
这个我觉得记录完整覆盖的起点，如果覆盖结束后，物体不见了，就直接掉这个起点的作为深度判定拿走的那一帧，之后继续用rgb往前找就行。
2.长期，最终：对输出等进行修改，比如im的视频输出可以去掉，中间没用的输出，调试用的输出，还有用处不大的文本数据等可以关掉。

260609
1.sam3d进行降面处理后，替代现有的unity端的显示人体骨骼逻辑，mesh以灰色半透明状态显示。推荐的比例，参考instantmesh的降面前后结果，sam3dbody的原始生成数据。
2.将拿取处理的整体状态机逻辑，写入独立的介绍文件。
3.对现在的服务器端所有处理流程的，详细输出格式（json记录文件的），目录文件格式，给出整体流程图和详细说明文档（写入文件）。并帮我看看两个模式怎么区分，一个是仅有最小数据专注生成，另一个是在最小数据外，带有额外debug报告的文件附录形式（写在文件末尾）。

260609-2
1.从现在开始，shigurei只按时间戳缓存rgb-d图片以及相机参数，时间这些必要信息，不单独记录frame，peopledetection等无用数据（直接在当前文件夹以文件保存，而不是套个文件夹）。对于重复的相机参数参数，不保留，默认往前搜索最新信息。去除unity端和服务器端对于shigurei的骨骼数据支持(包括配置和状态机判断中的手腕骨骼)。
2.删除/workspace/code/stages/model_event_tracking这个模块，以及和这个模块相关联的，我写的代码部分（不要改子仓库和docs的内容），这部分重写。
3.将stages里，那2个模块的相关的设定（除了目录约定外），从code目录下的config里，移动到对应文件夹下的settings.py内。shigurei，服务器相关等总设定（包括输入输出目录相关），才写在config里（注意event相关的的好像都写到项目里了）。


260611
1.shigurei的arucomarker基本不会变，所以在worker启动时，获取最新的图片后，自动尝试使用之后的，能看到的5次左右计算，随后更新历史记录。一般不会在更新前有任务，所以如果任务有需要的话，直接用历史记录就行。检查所有和shigurei的armarker相关逻辑，都要使用这个历史记录。

260611-2
1.使用选框（由模型的box推出）+模型中点（投影到shigurei的视角下）作为基础，在框内稳定后（依据depth即可），以那个选框+正点作为sam3的图像mask的导出逻辑，算出对象mask（复用现有已加载的那个模型）。替代现有的初始化3dmesh投影到shigurei再折腾mask的那个初始化逻辑。
2.在sam3的mask生成完毕后，根据深度图优先计算一个比较可信的包围盒（随便点就行），然后下发包围盒信息（两个角点），然后扔给unity，在本地对应位置先创建一个3d的不透明的box，然后在弹出窗口显示进度。
3.显示窗口需要放在模型到hololens终端的连线上，距离模型包围盒最外部20cm。根据hololens具体在模型的那个面比较近，决定是否更新位置。这个显示框固定就好。或者还有什么比较好的3d空间内显示图片或者信息的方式。
4.希望再现历史某一个时间点的历史摆放，这个涉及到已经摆放物体的检测，物体的移动监测，物体的消失检测。使用历史缓存图片和新的图片输入，然后找个模型输出移动啥的解决。

260618
0.整体的处理放在桌面上，所以可以根据depth图片+初始选框圈定范围，分割mask，来减少处理范围。
1.shigurei的存储加上yolo物体分割结果
2.使用稳定后的yolo分割结果（需要稳定，在当前帧对应选点有物体）+物体计算结束后，精确位置的点，进行后续拿取检测的mask初始化。（这里需要先进行尝试）
3.

260620
1.检查合理性
我需要修改目前主进程中的code/stages/shigure_history服务器。新的服务器应当有以下功能
1.以5fps接受rgb，depth数据，以视频压缩形式存储，chunk10s。yolo数据以不重复json格式存储。
2.启动服务器后自动开始记录，以整体最大10min为时间上线，过老的删除。yolo删除前需要确保下一个已经到目前最老时间之前（保证范围内都有yolo结果）
3.该服务器可以接受请求，请求为时间戳，返回对应的rgb，depth图片以及yolo数据，或者过老不存在？
4.如果要进行大范围查找的话，这个压缩和解压过程是否过重？

2.
拿走追踪初始化用到的是生成3d模型放置到以arucomarker为基准的空间后的（原先的位置和朝向），投影到shigure视角下的mask。
但是认为这个初始化不太好，没有必要非得这么走。
我觉得可以把这个整个和之后的现有流程作为第二选择（先和之后的新方法分离开，之后默认关闭（写道这个子模块的settings里）。
并按照下面修改：
优先使用3d模型中心坐标投影到shigure获得图片的视角下的那个图片的位置和深度，然后搜yolo对应时间之后的，稳定后的（多个收到结果存在），稳定开始第一帧开始使用这个作为mask记录范围内初始化深度。
之后直接持续使用yolo结果（比如同一个id的中心位置）进行是否移动粗筛。
如果突然消失，那么根据记录的初始化深度对比是否为遮挡。
现在对于yolo的id标签可以大致信任，如果标签记录的对应中心位置有一定移动，那么就是被拿走了；
在标签消失并非遮挡时，退化到旧的根据mask内根据最初记录深度对比的那个状态机进行验证（使用更简单版本，毕竟遮挡判断在前面）。
之后继续接入rgb图片，回溯一段时间进行判断精确拿走的帧（这块使用之前的那个就行）
后续不变


补充：
1.同意，需要根据模型位置和实际深度对准这是同一个模型（或许还可以加个简单的是否为同一物体的比对？）。2.yolo结果使用的是唯一yolo，这个使用前后3个相同就够了，实际的时间跨度大概在30s。3.初始化同样前后三个，其他同意。具体数值可以参考之前的那个旧的状态机？。
4.加防抖.粗筛现在有个问题，yolo更新速度过慢，可能需要在查出移动后，先用深度对比状态机从上一个yolo记录时间节点使用深度状态机细筛，之后再接着rgb查找帧精确定位。
5.我假设yolo有短暂遮挡的id不变的能力，所以假设引起条件只有短暂遮挡。
这种情况下，根据初始记录判断遮挡，并且如果后续yolo恢复，不用处理。如果判断为被拿走（深度根据旧状态机更深），那么判定为拿走，按rgb走。
如果是由于遮挡丢失id（旧位置没有id，并且原始记录mask范围内深度有一部分没变，有一部分更靠前，几乎没有靠后的），退化到旧状态机。
8.记录最新的到内存，有读取直接读，没有的话这个压缩完删除。9.对，引入yolo为了降低rgb和depth的读取量。
10.现在问题在于使用yolo计算mask如果初始化失败，可以尝试往后继续等一等，如果时间过长，重头退化到旧状态机，再不行判定为失败。


260621-1
1.需要加入：
1.在sam3的mask生成完毕后，根据深度图优先计算一个比较可信的包围盒（随便点就行），然后下发包围盒信息（两个角点），然后扔给unity，在本地对应位置先创建一个3d的不透明的box，然后在弹出窗口显示进度。
2.拿取显示窗口需要放在模型到hololens终端的连线上，距离模型包围盒最外部20cm。根据hololens具体在模型的那个面比较近，决定是否更新位置。这个显示框固定就好。或者还有什么比较好的3d空间内显示图片或者信息的方式。


260621-2
1.对于输入输出补充，希望可以记录hololens拍摄图片后，shigurei稳定的第一帧的rgbd图片以及当时yolo结果做备份。
2.检讨审查该需求并和我讨论具体内容（不要修改代码），需要加入：
希望再现历史某一个时间点的历史摆放，这个涉及到已经摆放物体的检测，物体的移动监测，物体的消失检测。使用历史缓存图片和新的图片输入，然后找个模型输出移动啥的解决。
由于我设计目标是只有新的图片和历史里模型放置的位置（这里需要计算新的模型和旧模型的相似度（也可能可以根据图片计算），最终目的是赋予一个独立的简单的另一个只在这里使用的id，能通过图片监控是否是同一个物体，或许可以通过这个跳过同一个模型生成步骤，直接进入后面的重新放置（这个目的是放置同时出现多个同一个物体的模型，导致之后的逻辑出现错误，所以尝试只保留最新的模型））

首先需要验证foundationpose对于shigurei视角下的，模型位姿计算是否准确。
由于模型被放到了新的位置。
我们假设shigurei的对同一个物体的id是一致的，但是这个需要做一个简单的验证（或许可以从图片和模型的相似度啥的？或者可以扔进去当初拍摄的图片和现在这个图片（都是带mask的），但是这样的话是不是角度太大就无法解决了），如果没有的话，尝试对yolo的所有结果使用3d模型跑一下是否有像的，如果都没有判定为没有；如果有的话，判定为id变更。
使用确认完同一个物体的，物体新的位置，以shigure视角带mask照片+模型+foundationpose，做一个新的临时位姿（如果物体已经存在在shigure视角中）。
我的想法是，hololens的手边菜单再加一个复原到拍摄时的物体位置的按钮。
如果这个物体不存在最好，直接以3d模型显示就行。
如果这个物体存在，那么首先把模型放到物体位置（使用shigure视角，按照前面说的处理，直接使用当前帧记录的，之前的最新的那个yolo记录就行），然后模型上面加一个旋转的蓝色正四面体（大小设定为边长10cm）用于提示这个物体在过去存在在不同的位置。
如果点击这个新位置（和当前位置重合）的模型的上面那个正四面体的话（这个正四面体放在新位置模型头顶），播放一个从当前位置飞到原先位姿的动画（具体移动时间，怎么显示，你帮我想下），再点击飞回。
如果点击放到原先位置的模型（无论是现在不存在直接放那的，还是现在存在，点击后飞回原先位置的那个），显示拿走的那个人体mesh和拿走那帧的图片。
如果物体还放在原来的位置，直接把模型放到原来的位置就好，就没有点击拿走啥的。

为了完成这个带同一物体唯一id的新的模型（注意，生成的3d模型有时候前一个会很离谱（很少），所以或许还是生成新的3d模型，计算两张拍摄图片的相似度（或许带上模型？），然后决定是使用原先的模型还是使用新的模型？
这个可能需要重建数据库和历史数据。帮我先备份旧数据，然后清空现有的流程输入输出记录数据。

260623-1
1.只验证一帧一个物体，这个可能需要在对应id物体暂时丢失时，根据之前数次的yolo数据判断是否可能是遮挡，如果是遮挡，可以复用前面的结果。
2.根据本地文档，这个没有验证嘛？
3.原始位姿这个可能需要算一下这两个图片或者图片和模型的相似度？或者什么别的方法，这样可以在hololens结果坐标周围几个候选中找个最可能的？防止有时候部分分割的mask被错误使用。
4.对，但是可以优先较为信任yoloid，稍微和模型或者图片什么验证下，如果错的离谱，轻量化根据yoloid结果搜索（这个结果头上转正6面体），如果再没有结果，就当没了。
5.这个不考虑，在只有两张图片，id不一定可信的情况下，无法保证。
6.missing判定要使用之前的数次yolo结果（同时要保证有效结果不应当出现移动），所以我认为很有可能就是没了
7.按你的来
8.可以
9.本身是逐级更细查找，使用的本身应该是现在yolo记录的，视频流提取出来的图像（根据现有未验证部分的主流程代码），应当是带时间戳的？
10.这个我记得主流程中是给了个比较大的内存需要范围，然后根据现有服务器内空余gpu显存判断哪个能用，然后自动分配？如果是空的的话，用多少？看看用量和时间消耗，决定是按你的多步搜索还是一次就够了。


260624
1.按照当前设计理念，根据data_backup/upload里的数据，跑下阈值范围试试。（注意，旧版数据比较老的部分可能有部分计算数据缺失或者不可信，如果必要数据部分缺失或者不可信，跳过不纳入评估范围）（注意，极端结果可能有参考价值）
分组1喷火龙：20260317_045027_537630Z_color，20260324_025038_028533Z_color，
20260324_031851_292561Z_color，20260324_035244_038896Z_color，
20260324_035738_667571Z_color，20260324_035911_672850Z_color，
20260324_040441_641714Z_color，20260324_040756_158732Z_color，
20260324_041218_378309Z_color，20260324_041420_308982Z_color，
20260324_041808_606442Z_color，20260324_042139_302949Z_color，
20260324_042816_975361Z_color，20260324_042955_296105Z_color，
20260328_080701_586889Z_color，20260401_064321_300665Z_color，
20260401_095419_366541Z_color，20260401_095651_542083Z_color，
20260409_013930_832632Z_color，20260409_034913_642660Z_color，
20260409_035602_254544Z_color，20260409_043416_187341Z_color，
20260409_062336_779577Z_color，20260409_081228_799423Z_color，
20260409_082708_195621Z_color，20260413_053708_276083Z_color，
20260413_081218_408632Z_color，20260413_083851_393266Z_color，
20260413_084917_367185Z_color，20260505_131327_617210Z_color，
20260514_100949_346596Z_color，20260518_090325_198433Z_color，
20260605_043320_957058Z_color，20260605_053029_692366Z_color，
20260605_092639_399392Z_color，

分组2熊本熊：20260317_070157_101152Z_color，20260324_073346_077597Z_color，
20260414_015257_813098Z_color，20260414_021529_544462Z_color，
20260416_043122_678007Z_color，20260423_054952_248230Z_color，
20260423_055626_798799Z_color，20260423_081825_162650Z_color，
20260423_082609_761786Z_color，20260423_084434_459448Z_color，
20260427_084351_402162Z_color，20260427_090355_711593Z_color，
20260429_063443_356412Z_color，20260505_131301_558790Z_color，
20260511_082232_800422Z_color，20260513_105015_520232Z_color，
20260514_005641_940791Z_color，20260514_025401_310793Z_color，
20260514_100839_457912Z_color，20260527_235220_834961Z_color，
20260603_100841_990833Z_color，20260604_031142_811506Z_color，
20260605_053043_996133Z_color，20260605_092623_759164Z_color，


分组3皮卡丘：20260328_081143_065919Z_color，20260406_074741_480379Z_color，
20260416_005239_608413Z_color，20260416_023240_590019Z_color，
20260416_041944_575113Z_color，20260416_042130_001041Z_color，
20260427_095607_688215Z_color，20260429_055052_443670Z_color，
20260505_111936_820121Z_color，20260505_121327_870434Z_color，
20260505_131144_510269Z_color，20260511_082300_585933Z_color，
20260513_104915_464994Z_color，20260514_100814_234647Z_color，
20260527_235236_372420Z_color，20260604_031202_386908Z_color，
20260605_043307_227560Z_color，20260605_092654_894340Z_color，


分组4松鼠？：20260328_082037_577011Z_color，20260505_121357_276812Z_color，
20260505_131226_439032Z_color，20260511_114203_494201Z_color，
20260513_104952_339777Z_color，20260528_030034_849895Z_color，

分组5蓝狐狸？：20260416_043808_903673Z_color，20260429_063938_180906Z_color，
20260505_131406_415805Z_color，20260511_114130_664565Z_color，
20260513_133820_710164Z_color，20260514_100904_405925Z_color，
20260527_235200_600540Z_color，20260601_035756_274239Z_color，
20260603_094036_195188Z_color，20260605_045142_919588Z_color，


分组6多面体球：20260416_044557_204900Z_color，20260511_121057_967835Z_color，

分组7白狗：20260429_064556_556964Z_color，
分组8安卓：20260507_005037_262014Z_color
分组9白鸟：20260507_005108_214876Z_color
分组10湿纸巾圆柱罐子：20260511_121207_454855Z_color，20260514_025426_860484Z_color，

分组11另一个除菌罐子：20260513_133921_359288Z_color，
分组12会议用摄像头柱体：20260513_134002_340986Z_color，20260514_005618_772613Z_color，
20260514_025333_139171Z_color，
分组13任天堂炸弹：20260518_090417_929434Z_color，
分组14机箱：20260528_025214_494796Z_color，
分组15橙色椅子：20260528_025703_996644Z_color
分组16加湿空气净化器：20260601_035722_958566Z_color，20260603_094240_631284Z_color，

分组17蓝椅子：20260601_035839_243537Z_color，20260603_094121_660672Z_color，
20260603_100908_781092Z_color，

分组18打印机：20260601_035909_630402Z_color，20260603_100928_590458Z_color，

分组19大屏幕（这个表面纹理多半对不上，作为弱参考用吧，降低权重）：20260601_035958_396538Z_color，20260603_094053_484984Z_color

分组20红背椅子：20260603_100957_473210Z_color，
分组21绿椅子：20260603_101033_308238Z_color，20260605_092716_228601Z_color


输入输出整理：
1.