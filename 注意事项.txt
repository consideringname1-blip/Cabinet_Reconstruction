260303
1.实装后的深度图像校准部分，未实际安装conda运行环境，也未对环境进行备份
2.程序执行后，json文件被修改，但是未重新读取（暂时问题不大）
3.新实装的HoloLens2数据下载和深度图像校准部分代码未执行验证过

260309
server环境需要安装
pip install numpy opencv-python av requests
pip install pyzdepth

260315
1.模型生成已写道sam3，未完成环境相关config的内容填写，可能未搭建完环境
2.需要把除了最初的json写入外的所有流程放到task_worker.py里
3.生成taskid或许不用这么多东西
4.taskid的列表和状态或许可以写到本地文件里，最少需要taskid，对应json，状态

260315-2
1.未完成sam相关环境配置，blender不知晓
2.缺少重定位和大小缩放相关代码

260317
在该文件中，使用probreg，写一个获得可以把模型缩放到正确大小和旋转姿势，并且有正确位置的代码，该代码接受task_worker管理，在objectalignment步骤中被使用。该代码接受json地址，使用"PVCamera""k"获取将要使用的，用于深度图投影的相机内参矩阵，"PVCamera""pose"是设备位姿

260328
1.生成的模型，现在在计算结束后是显示在眼前的，就像是在拍照时的视角的镜像（以模型中点为中心）。似乎是由于拍摄时得到的，以当时的相机为原点的点云坐标数据，被直接应用到了新的相机位置和姿势上。这就导致了显示在正前方的模型，看着很像是在模型的对面的位置拍摄的感觉。
2.最后的那个土拨鼠玩偶，似乎因为距离过远导致depth相机数据出现问题（怀疑距离1m左右）
3.在尝试进行第二次拍照时，出现不明崩溃，这个崩溃也见于同步启动相机时，或者上传等待时间过长时。
4.怀疑可能导致崩溃原因
[worker] completed task: 33383d81-80f8-4d65-8120-d7feb836bc4b
10.40.1.132 - - [28/Mar/2026 08:22:50] "GET /check/?task_id=33383d81-80f8-4d65-8120-d7feb836bc4b HTTP/1.1" 200 -
10.40.1.132 - - [28/Mar/2026 08:22:50] "GET /files/fbx/20260328_082037_577011Z_sam3_color.fbx HTTP/1.1" 200 -
[ERROR] /generate exception: 413 Request Entity Too Large: The data value transmitted exceeds the capacity limit.
10.40.1.132 - - [28/Mar/2026 08:27:06] "POST /generate HTTP/1.1" 400 -

260409
1.在hololens启动时加上时间戳获取，每次上传时同步传下，作为这次启动的标志，为之后使用mark进行世界坐标保存做准备
2.在server上加上识别对应的模块，先判断框选范围内有无完整的这个标，如果有，在当前一直在跑的taskworker旁路进行坐标重计算
3.坐标重计算需要修改数据库结构，加上以启动时间戳为基准的部分，向内写入mark的坐标（作为多次启动时模型加载的基准位置）
4.修改unity和server相关代码，允许使用上一次数据

1.0
1.增加shigurei的位置记录
2.unity增加滑块，物品表，可以放置物体并进行位置追踪
3.朝向是个很大的问题，估计无法理解朝向（待议）

260412
1.config里增加可选渲染结果选项（对于instantmesh的video生成也是一样）
2.按照目前的预览渲染的链路（包括模型导入，fbx生成，icp结果等相关坐标变换），重新检查最终的模型位姿变换结果

260414
我修改了一部分的pvcamera的旋转矩阵提取四元数后的z反转问题：“def compute_world_pose(task: dict) -> dict[str, list[float]]:
    ...
    world_quat = rotation_matrix_to_quat_xyzw(world_rotation)
    world_quat[2] *= -1.0
    world_quat = normalize_quat_xyzw(world_quat)”和“def serialize_pose(rotation: np.ndarray, translation: np.ndarray, coordinate_basis: str) -> dict:
    ...
    quat_xyzw = rotation_matrix_to_quat_xyzw(rotation)
    quat_xyzw[2] *= -1.0
    quat_xyzw = normalize_quat_xyzw(quat_xyzw)”
但是好像只修改了相机的部分，并没有修正物体的部分。按照这个，继续修改这个链路中的相关项（只反转这个z值，不要做任何其他事情）。

260423
1.修改文件目录以及git和dvc追踪文件夹：sotsuken-hololens2-system\data\aruco内改成一个git追踪的包含原始marker图片ar_marker_7x7_1.png和数据的aruco.json的文件夹，和一个dvc追踪的放计算数据文件夹，并在setup-dvc-gdrive.txt中的日常流程：上传：中进行修正
2.清理git追踪的sotsuken-hololens2-system\data\aruco内历史数据，直接从最新版开始追踪
3.检查database里的数据，指出并修正程序中错误的地方
4.在拍摄距离过远时，显示上传文件过大，深度怀疑是深度图片的问题，尝试在网上搜索相关问题并修正
5.database里有一次好像出现图片里有marker，却没识别出来的情况，这种应该怎么解决？新增一个marker按钮吗？

260423-2
说明：近距离AHAT的“距离过远/上传文件过大”拦截目前先保持不启用；相关代码只保留深度清洗与统计，不主动阻止上传。若之后要恢复拦截，需要同时重新启用Unity端和server端的AHAT上传拦截。
1.深度清洗似乎还是解决不了，会被统计拦截
2.急需添加相关按钮专门进行marker适配
3.check依旧无法停止，可能需要重写这块的停止条件等
4.加载最新一次含有有效marker数据的那块还是有问题，似乎写成加载上次有效数据了

260427
1.在拍摄mark后，unity还是一直在check，但是本地有坐标系了，看看check的流程和终止条件
2.setting没有正常显示，怀疑unity没有绑定啥的

260427-1
1.可改进：
1.1.marker的微小误差会导致模型位置出现巨大变动
1.2.本地只能存储一个模型，只能历史访问一个模型
1.3.缺少long深度相关整个的流程
2.之后需要：
2.1.接入项目数据库绑定，历史位置查询功能
2.2.最重要的是需要能够知道这是同一个东西
2.2.1.预计要使用shigurei的别人的项目，需要能够发送图片+文本（或者声音），服务器获得查询结果。
2.2.2.shigurei需要深度图进行icp，以确定位置和旋转角度（根据深度图精度，怀疑还需要渲染比对），但是时间会很长
2.2.3.可以先简单的放在那里，icp扔后台另一个慢慢算。也可以稍微聪明点，预先算好，物体移动后就开始算
2.3.shigurei访问

260430
1.电脑和服务器实装shigurei
2.为了获取shigurei的记录数据，需要目前marker为基准的坐标系，和shigurei为基准的深度数据+图片位置的两个变换函数
3.服务器应该会记录以shigurei为基准的位置（图片位置和深度）以及时间，作为和shigurei的同步依据
4.（shigurei的rgb摄像头和depth摄像头坐标系是否统一需要调查）
5.hololens2实装手指的位置（指尖延伸到障碍物位置，之后以次为基准向上半球搜索）
以此应该可以最基础再现，手指位置附近曾经存在的物体
（但是需要考虑数据库范围，可能需要指定数据范围）
6.模型面数可缩少调查
7.icp点数可简化
8.再贴几个marker（待议）

260502
这部分功能目标是可以在现实中指一个位置，然后显示历史里曾在此位置的模型（历史拍摄并建模的模型）
1.新建database表，用于存放模型在世界中的包围盒信息（AABB）（在上传生成时）
该包围盒关联的database的表应包含上传时间，模型名称，包围盒8个角点位置（以armarker（统一坐标系）为基准），三个部分
2.在unity端，新建脚本，挂载需要你告诉我怎么做。
该脚本目的为，只允许右手（或者左手），在unity界面可选但是固定，发射射线到终点平面（现实世界障碍），程序将在unity打开时强制该射线显示。
程序将会在某个函数触发后，在等待3s（unity可设定时间）后获取当前时间下的，食指起点坐标，射线方向，射线终点坐标，上传服务器解析。
3.服务器解析食指起点坐标和射线方向，以终点坐标为截止位置（如无，设置最远距离10m；或者已经超出指尖到最远模型角点距离），路径上碰到的第一个模型的包围盒的六个面中的任何一个（内外都可）。
4.对包围盒设置两个检索标准，一个是默认的最新5个（unity写死），另一个是某段时间之内（两个函数，但是第二个写完留着就行）
5.该功能和之前的armarker相关请求，我希望可以另开一个worker解决（高优先级，并行），所以需要对于生成中的模型，准备好锁来防止生成时出现armarker同时更改数据库同一个位置（虽然感觉影响不大，或许单纯的标注为未完成的这种，armarker直接跳过，等生成完了自己调重写就行？这样marker更新也就问题不大（帮我检查这部分思路））
6.加一个函数，该函数为开关（点一下开，再点一下关），包围盒计算虽然在云端完成，但是我还是希望在调试时，本地可以看到。
在该开关打开时，我可以看到检索范围内的包围盒位置（服务器下载最新，并且设置为显示）。关闭时记得清除。


260505-1
debug
1.hololens自带的射线正常显示，但是另一枚射线（直线）追踪的太好了（太容易动了，并且没有在平面位置正确停止），我不需要这条射线，以hololens的右手默认的那条为准（现在hololens2只手都默认的有射线，只有右手有一个白色光柱，非常灵敏的，指尖发出的）（两条hololens默认的射线保留）
2.resest selectionbox，点击仍旧没反应，我希望是在弹出这个窗口后，能改变窗口位置（毕竟窗口可能在不好点的位置）
3.点击左手设置菜单后，非常卡，甚至程序感觉直接无响应了，之后过一段时间又好了；拍照倒计时结束后，也突然感觉像程序无响应，我的手部默认显示的东西都没了。
4.在上传两个模型待处理（处理中）后，直接上传armarker，显示错误，数据库里也没有第二次的模型有关的任务数据以及后面的armarker数据（armarker可能是程序设计如此，但模型生成不应该）（我的本意是可以依次在未完成状态上传多个图片到服务器队列处理，并且可能在某个时候上传下armarker进行坐标标定，处理完成后自动返回数据（armarker优先度高））
5.射线没有倒计时，结束后一直显示nohit（明明hololens的那条射线已经打到模型上了）

260505-2
1.reset selectionbox 指的是重置（Canvas Selection box）；设置窗口（CanvasSettings），还是点一下开，再点一下关，开的时候放在眼前0.8m处的设定
2.我希望taskworker不是一条跑到黑，而是在一个阶段结束，直接切到优先级最高的上面（注意保留armarker的最高优先级）
3.增加接口，清除本地保留模型（给出位置和名称，我随后加上）
4.现在hololens在上一个内容处理未结束，请求新的内容后，旧的内容无法自动加载（这个需要商定计划讨论），并且有一个box，没有正确显示（有没有可能当时没有拍armarker？），这个应该可以自动补上的。

260505-3
解决如下问题
1.reset selectionbox 指的是重置（Canvas Selection box），在拖动方块选择范围阶段，无法重置位置
2.扔回队列是扔到队列最上面，这样可以保证优先处理完成一个，并且可以在有armarker后，优先处理armarker
3.armarker生成结束，返回数据不显示armarker的原点的那个坐标轴了（准确来讲，无法正确接收到返回的数据）

260505-4
疑问：最终的那个box，是从那个结果里生成的，instantmesh还是后面抽象的那个
讨论如下修改
1.instantmesh生成的3d模型有的时候在某个模型外一点有杂音（突然就那一小块），帮我检测并且删去（在instantmesh生成模型后）
2.把icp部分，换成另一块（使用预留的那个跳过选项进行切换），这一块逻辑如下：
根据摄像机距离，和推测物体模型的缩放结果（会得到一个新的包围盒），去假设这个距离是包围盒前表面，进行模型放置（在摄像机到图片点云重心的延长线上，延长距离根据包围盒估算，试试厚度的一半）

260507
1.射线发射方向很有问题，似乎是视线前方而不是手指位置（hololens射线默认就这样吗）。
2.射线碰到空间上的障碍物，没有停留在表面上而是穿过去了（有的有反应，有的没反应）。
3.现在还是没有办法在armarker生成结束后自动下载，我通过 showlastmodel的逻辑成功活得到了marker坐标并显示，检查armarker问题。
4.我不希望在selection关闭时，点击resetselectionbox出现任何反应，只在打开时按照现有逻辑放置，也不需要操作开关；需要操作开关的是setting按钮。

260511
1.icp阶段（也可能是pose）还是没有明显加速
2.有一个模型生成结束后，hololens没有自动下载模型（在同时上传两个模型进行处理的时候）（多个模型生成结束也是没有自动下载）
--前俩未验证
3.在现有流水线上，加上切换sam3d的逻辑并且切过去（sam3d好像出的是ply点云，可能需要继续验证以及商议）。

260511-1
（已更新数据库）
1.拍摄armarker后，服务器已经显示已完成，但是本地还是显示start，也没有下载，也没有显示。
2.在该情况下，点击pointobject后，自动下载并显示了历史的5个模型，这不是我希望的（但是armarker显示了出来），之后清理所有模型缓存后，又变正常了
3.在有本地armarker坐标的情况下，连续拍摄两个模型，并没有正确下载，一直在model_polling_start
4.但是生成结束后，用手指显示那个能正常下载

260511-2
1.点击resect selection box 不应该关闭已经打开的selectionbox（去掉那个管拖动方块上传图片加box的那个的点击关闭的逻辑）
2.程序启动时没有在头上生成那个坐标轴（最新一次开始出现该问题），同时armarker依旧在卡。
3.在第一次尝试手指方向显示时，下载了armarker，然后设备启动位置以及armarker都出现坐标轴了。
4.随后可以正常下载并显示对应的模型
5.show last model还是希望显示历史最新的全部5个（其他好像没有问题）
6.模型生成完成后依旧没有自动下载。这块我怀疑和armarker的自动请求逻辑都有同一个问题。
7.补充：自动刷新跨 session，但是只能刷新本地已有的（正在显示的），没有的不要动。
8.补充：上次额度用完了，不知道改到了那里，综合git仓库记录和上次的记录，继续改

260518
1.不知道为什么，新加的输出显示使用设备为cpu，日志如下
[STAGE] object_alignment : /workspace/data/upload/20260518_090325_198433Z_meta.json
[INFO] object_alignment : backend=cpu alignment_mode=foundationpose icp_enabled=False points=43474/3600 model_fit_points=0 discarded=3548 scale=0.225904 confidence=0.921 rmse=0.007180 preview=not-generated
[INFO] pose-local-rh   : pos=[0.003672159742563963, -0.02133191004395485, -0.5438885688781738] quat=[0.266676190029047, -0.035103126283652746, 0.010228804802634116, 0.9630923900379522]
[OK] object_alignment
2.需要后续引入shigurei